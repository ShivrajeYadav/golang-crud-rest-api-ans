name: Build and Push Docker Image

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: golang-crud-rest-api-ans-image
  GITHUB_TOKEN: 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

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: github.com/${{ github.repository }}/${{ env.IMAGE_NAME }}:latest
          builder: ${{ env.BUILDKIT_BUILDER }}
          registry: github.com
          username: shivrajeyadav
          password: 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

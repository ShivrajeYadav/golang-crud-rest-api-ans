name: Build and Push Docker Image

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: golang-crud-rest-api-ans-image
  GITHUB_TOKEN: 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

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: github.com/${{ github.repository }}/${{ env.IMAGE_NAME }}:latest
          builder: ${{ env.BUILDKIT_BUILDER }}
          registry: github.com
          username: shivrajeyadav
          password: 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

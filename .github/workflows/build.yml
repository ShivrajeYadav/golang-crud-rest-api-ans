name: Build and Push Docker Image

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: golang-crud-rest-api-ans-image
  GITHUB_TOKEN: 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

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: docker.pkg.github.com/${{ github.repository }}/${{ env.IMAGE_NAME }}:latest
          builder: ${{ env.BUILDKIT_BUILDER }}
          registry: github.com
          username: ${{ github.actor }}
          password: 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
